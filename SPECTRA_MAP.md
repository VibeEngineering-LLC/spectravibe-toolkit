# Карта спектров

Собрано генератором `scripts/convert/build_spectra_map.py` по индексу git —
здесь только то, что действительно опубликовано. Подробные описи с паспортными
активностями лежат в `detectors/<класс>/reference_spectra/README.md` и `INDEX.json`.

Всего в репозитории: **251 файлов BecqMoni XML** и 678 исходных `.spe`
по 11 классам детекторов, 79 МБ.

## Структура

```
spectravibe-toolkit/
├── SKILL.md, ARCH.md, FORMAT_REGISTRY.md, NOTES_v1.7_methodology.md
├── scripts/          библиотека gamma/, CLI-утилиты, конвертеры
├── tests/            тесты по шагам конвейера
├── data/             алиасы, нуклиды, XRF-линии, tsc_lookup
├── references/       контракт демо-отчёта, кэш IAEA, ground-truth ЛСРМ
└── detectors/        251 XML · 678 .spe
```

## Спектры по классам

| Класс | Тип детектора | `.spe` | **XML** | МБ |
|---|---|---:|---:|---:|
| `Handy_LaBr` | LaBr₃ | 41 | **41** | 2.7 |
| `Gamma-1S` | NaI(Tl) 63×63, УДС-ГЦ | 85 | **40** | 4.1 |
| `Handy_HPGe` | HPGe GMX | 39 | **39** | 23.0 |
| `Handy_NaI` | NaI | 33 | **33** | 2.6 |
| `GP_HPGe20` | HPGe 20 % общего назначения | 32 | **32** | 10.3 |
| `NM_HPGe20` | HPGe коаксиальный 20 % | 427 | **30** | 26.5 |
| `AtomSpectra` | сцинтилляционный, фикстуры | — | **11** | 3.4 |
| `Simple_HPGe` | HPGe планарный, демо | 9 | **9** | 5.2 |
| `Simple_NaI` | NaI | 6 | **6** | 0.3 |
| `Simple_TeCd` | CdTe / CZT | 6 | **6** | 1.0 |
| `RadiaCode_103` | CsI(Tl), апатит и камень | — | **4** | 0.2 |
| **Итого** | | **678** | **251** | **79.3** |

### Что лежит локально и в git не идёт

В репозитории публикуются только **курированные** наборы Gamma-1S —
`reference_kits/` и `reference_kits_becqmoni/`: пары «образец + фон» по
геометриям, с описью, паспортными активностями и кривыми эффективности.
Массовая выгрузка рабочего дерева ЛСРМ остаётся на машине оператора.

- `detectors/Gamma-1S/reference_spectra/lsrm/` — 407 файлов
- `detectors/Gamma-1S/reference_spectra/becqmoni/` — 407 файлов
- `detectors/Gamma-1S/reference_spectra/background/` — 357 файлов

Пересобирается из исходного дерева одной командой:

```bash
python scripts/convert/lsrm_tree_publish.py --class Gamma-1S
```

### Почему у NM_HPGe20 разрыв между `.spe` и XML

Не сконвертированы объёмные исследовательские коллекции — Pu/U LNHB, INR,
IPPPE, kpti, около 400 спектров. У HPGe 16 384 канала, один XML весит ~590 КБ
против 33 КБ исходника; это добавило бы ≈250 МБ производных файлов. Исходники
опубликованы, экспорт собирается по требованию:

```bash
python scripts/convert/lsrm_tree_publish.py --class NM_HPGe20 --xml-all
```

---

## ⚠️ ВАЖНО: 9 спектров BecqMoni прочитает с неверной шкалой

`PolynomialEnergyCalibration.CheckCalibration()` отвергает любой порядок выше
четвёртого:

```csharp
if (this.polynomialOrder > 4 || this.polynomialOrder < 1) { return false; }
```

Дальше `DocumentManager.CheckDocument()` пытается спасти положение, но понижает
степень только за счёт нулевых старших коэффициентов. У настоящего полинома 5-й
степени их нет, поэтому калибровка заменяется на **дефолтную** — файл открывается,
шкала теряется, сообщения об ошибке нет.

У этих спектров ЛСРМ записал калибровку 5-й степени. Коэффициенты сохранены как
в источнике.

| Класс | Файл | Где | Спектр | Фон |
|---|---|---|---:|---:|
| `Gamma-1S` | `Черника сушеная ORG-01_1_Дента-120мл_0cm.xml` | локально | 5 | 2 |
| `GP_HPGe20` | `Bckg_1.xml` | в git | 5 | — |
| `GP_HPGe20` | `Bckg_15.xml` | в git | 5 | — |
| `GP_HPGe20` | `m08085_mix09.xml` | в git | 5 | — |
| `GP_HPGe20` | `Y88-SRC-05-25cm.xml` | в git | 5 | — |
| `NM_HPGe20` | `Bckg_1.xml` | в git | 5 | — |
| `NM_HPGe20` | `Bckg_15.xml` | в git | 5 | — |
| `NM_HPGe20` | `m08085_mix09.xml` | в git | 5 | — |
| `NM_HPGe20` | `Y88-SRC-05-25cm.xml` | в git | 5 | — |

«локально» — файл лежит на машине оператора, в репозиторий не публикуется.

**Что делать.** Берите калибровку из `INDEX.json` соответствующего класса —
поле `energy_cal`, коэффициенты от младшей степени к старшей:

```
E(n) = a₀ + a₁·n + a₂·n² + a₃·n³ + a₄·n⁴ + a₅·n⁵
```

Найти такие файлы можно по флагу `becqmoni_reads_as_linear` в том же индексе.

**Фон проверяется отдельно.** BecqMoni валидирует `<EnergySpectrum>` и
`<BackgroundEnergySpectrum>` как две независимые калибровки, поэтому подшитый фон
со степенью 5 губил шкалу даже у спектра с линейной калибровкой. Такой фон больше
не вшивается — остаётся только ссылка `<BackgroundSpectrumFile>`, а признак стоит
в описи полем `background_dropped_high_order`. Подробности — в
[`detectors/CHANGELOG.md`](detectors/CHANGELOG.md).

**Почему не исправлено пересчётом.** Приведение к 4-й степени было реализовано
и отклонено: на HPGe-кривых оно ложится в пределах 0,21 кэВ, но на сильно
изогнутой NaI-калибровке лучшее приближение 4-й степени промахивается на 57 кэВ.
Молча менять сохранённую калибровку хуже, чем сообщить о проблеме — решение
оператора от 2026-07-27. Простое отбрасывание старшего члена ещё хуже: на канале
8192 оно даёт ошибку 53 кэВ, а на NaI-случае — более 14 000 кэВ.

---

## Прочие особенности форматов

Конвертеры обходят ещё два дефекта BecqMoni, оба задокументированы в коде:

- **Единицы `SampleInfo`** — `Weight` в килограммах, `Volume` в литрах, диапазон
  контрола 0,001…100. ЛСРМ даёт граммы и миллилитры; без деления на 1000
  приложение падает с `ArgumentOutOfRangeException` до отрисовки документа
  (`gamma/io/becqmoni_xml.py`, BUG-BQ2).
- **GUID прибора** выводится из имени детектора через `uuid5`, иначе каждая
  перегенерация создавала бы в BecqMoni новый прибор.

## Где лежит фон

Фоновые измерения каждого детектора собраны в отдельную ветку
`reference_spectra/background/` — `lsrm/` для исходников и `becqmoni/` для XML,
с сохранением исходной иерархии. Измерения на них ссылаются по имени файла.

## Обозначения

Приборы и источники обозначены внутренними метками вида `SN-nn` и `SRC-nn` —
одна метка на один прибор или источник, устойчиво между сборками.
